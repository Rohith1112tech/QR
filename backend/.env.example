# Server Configuration
PORT=5000
FRONTEND_URL=http://localhost:5173

# Database configuration (PostgreSQL)
# Leave blank to automatically fall back to local JSON database (database.json)
DATABASE_URL=

# Cloudinary configuration
# Leave blank to automatically fall back to saving uploads in local "/uploads" directory
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
